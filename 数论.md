# 数论学习笔记

## 0x00 整除

### 1. 定义
若整数 $n$ 除以整数 $d$ 的余数为 0，即 $d$ 能整除 $n$，则称 $d$ 是 $n$ 的约数，$n$ 是 $d$ 的倍数，记为 $d \mid n$。

### 2. 整除的性质
* **性质 00.1.1 (传递性)**: 若 $a \mid b$ 且 $b \mid c$，则 $a \mid c$。
* **性质 00.1.2**: 若 $a \mid b$，则 $a \mid bc$，其中 $c$ 为任意整数。
* **性质 00.1.3 (线性运算)**: 若 $a \mid b$ 且 $a \mid c$，则 $a \mid (kb \pm lc)$。
    * *拓展*: 如果 $k$ 和 $l$ 互质，仅当 $a=1$ 时能整除 $k+l$ 与 $k \times l$。
* **性质 00.1.4 (反对称性)**: 若 $a \mid b$ 且 $b \mid a$，则 $a = \pm b$。
* **性质 00.1.5**: 若 $a = kb \pm c$，则 $a$ 和 $b$ 的公因数与 $b$ 和 $c$ 的公因数完全相同。
* **性质 00.1.6**: 若 $a \mid bc$，且 $a$ 与 $c$ 互质，则 $a \mid b$。

### 3. 典型例题
* **题目 (1987年初中数学联赛)**: 设 $x, y, z$ 为整数，若 $11 \mid (7x+2y-5z)$，求证：$11 \mid (3x-7y+12z)$。

---

## 0x10 整除相关：素数 (Prime Numbers)

### 1. 定义与定理
* **定义**: 素数（又称质数）是只有 1 和它本身两个因数的数。1 既非素数也非合数。2 是唯一的偶素数。
* **素数定理**: 在自然数集中，小于 $n$ 的质数约有 $\frac{n}{\ln n}$ 个。

### 2. 素数的判定 (0x11.1)

#### A. 试除法
* **原理**: 若 $n$ 不是素数，必有一个小于等于 $\sqrt{n}$ 的因子。
* **时间复杂度**: $O(\sqrt{n})$。
* **代码**:
    ```cpp
    inline bool is_prime(int x) {
        if (x < 2) return false;
        for (register int i = 2; i * i <= x; ++i)
            if (x % i == 0) return false;
        return true;
    }
    ```

#### B. $kn + i$ 法 (优化)
* **原理**: 枚举形如 $kn+i$ 的数。例如 $k=6$ 时，只需枚举 $6n+1$ 和 $6n+5$，因为 $6n+2, 6n+3, 6n+4$ 显然包含因子 2 或 3。
* **模板 (k=30)**: 仅需检测与 30 互质的余数，效率提升明显。

### 3. 素数的筛法 (0x11.2)

#### A. 埃拉托色尼筛法 (Eratosthenes Sieve)
* **原理**: 从小到大枚举，把当前数的所有倍数标记为合数。
* **时间复杂度**: $O(n \log \log n)$。
* **代码**:
    ```cpp
    int v[N];
    void primes(int n) {
        memset(v, 0, sizeof v);
        for (int i = 2; i <= n; ++i) {
            if (v[i]) continue; // i是素数
            for (int j = i; j <= n / i; ++j) v[i * j] = 1;
        }
    }
    ```

#### B. 线性筛 (Euler Sieve)
* **原理**: 保证每个合数只会被它的**最小质因子**筛掉一次。
* **核心**: 当 `i % primes[j] == 0` 时 `break`，防止重复筛选。
* **代码**:
    ```cpp
    const int N = 10005;
    int n, primes[N], cnt;
    bool vis[N];
    inline void get_prime() {
        for (register int i = 2; i <= n; i++) {
            if (!vis[i]) primes[++cnt] = i;
            for (register int j = 1; j <= cnt && i * primes[j] <= n; ++j) {
                vis[i * primes[j]] = 1;
                if (i % primes[j] == 0) break;
            }
        }
    }
    ```

### 4. 重点例题
* **Problem B 质数距离 (AcWing 196)**
    * **题意**: 给定区间 $[L, R]$，求相邻质数差最大/最小对。$L, R \le 2^{31}-1, R-L \le 10^6$。
    * **思路 (二次筛法)**:
        1. 筛出 $\sqrt{2^{31}-1} \approx 50000$ 内的素数。
        2. 用这些素数筛掉 $[L, R]$ 范围内的合数。
        3. 标记倍数时起点计算：`j = max(p * 2, (l + p - 1) / p * p)`。

---

## 0x12 唯一分解定理 (算术基本定理)

### 1. 定理内容
任何大于 1 的整数 $n$ 可唯一分解为质数乘积：
$$n = \prod_{i=1}^{m} p_i^{C_i} = p_1^{C_1} \times p_2^{C_2} \times \dots \times p_m^{C_m}$$
其中 $p_i$ 为递增质数，$C_i$ 为正整数。

### 2. 推论 (GCD 与 LCM)
若 $n = \prod p_i^{\alpha_i}, m = \prod p_i^{\beta_i}$，则：
* $\gcd(n, m) = \prod p_i^{\min(\alpha_i, \beta_i)}$
* $\text{lcm}(n, m) = \prod p_i^{\max(\alpha_i, \beta_i)}$。

### 3. 分解算法
* **试除法**: $O(\sqrt{n})$，枚举因子并除尽。
* **线性筛预处理**: 记录 `pre[x]` (最小质因子)，可实现快速分解。

### 4. 例题：阶乘分解 (AcWing 197)
* **思路**: $N!$ 中质因子 $p$ 的个数 = $\sum_{k=1} \lfloor \frac{N}{p^k} \rfloor$。

---

## 0x13 最大公因数与最小公倍数

### 1. 约数 (Divisors)
* **约数个数公式**: $\prod (C_i + 1)$。
* **约数和公式**: $\prod (\sum_{j=0}^{C_i} p_i^j)$。
* **试除法求约数集合**: $O(\sqrt{n})$，成对收集因子。

### 2. GCD 与 LCM 性质
* $\gcd(a, b) = \gcd(b, a \pmod b)$ (欧几里得算法)。
* $\gcd(a, b) \times \text{lcm}(a, b) = a \times b$。
* $\gcd(F_n, F_m) = F_{\gcd(n, m)}$ (斐波那契数列性质)。

### 3. 例题：约数之和 (AcWing 97)
* **题意**: 求 $A^B$ 的所有约数之和 $\pmod{9901}$。
* **思路**: 结合约数和公式与等比数列求和。若 $p-1 \not\equiv 0 \pmod{9901}$，使用逆元；否则特判。

---

## 0x14 互质与欧拉函数

### 1. 欧拉函数 $\phi(N)$
* **定义**: $1 \dots N$ 中与 $N$ 互质的数的个数。
* **公式**: $\phi(N) = N \times \prod_{p|N} (1 - \frac{1}{p})$。

### 2. 核心性质
* **积性**: 若 $\gcd(a, b)=1$，则 $\phi(ab) = \phi(a)\phi(b)$。
* **和的性质**: $\sum_{d \mid n} \phi(d) = n$。
* **递推求法**:
    * $p \mid n, p^2 \mid n \Rightarrow \phi(n) = \phi(n/p) \times p$。
    * $p \mid n, p^2 \nmid n \Rightarrow \phi(n) = \phi(n/p) \times (p-1)$。

### 3. 例题：仪仗队 (SDOI2008)
* **转化**: $N \times N$ 方阵中从 (0,0) 能看到的点，本质是求 $\gcd(x, y)=1$ 的点对数，即欧拉函数的前缀和。

---

## 0x20 同余 (Modular Arithmetic)

### 1. 运算性质
* $(a + b) \% p = ((a \% p) + (b \% p)) \% p$。
* 若 $ac \equiv bc \pmod m$ 且 $\gcd(c, m)=1$，则 $a \equiv b \pmod m$。

### 2. 欧拉定理
* 若 $\gcd(a, m) = 1$，则 $a^{\phi(m)} \equiv 1 \pmod m$。

### 3. 扩展欧拉定理 (欧拉降幂)
用于处理指数极大的情况：
$$
a^b \equiv \begin{cases} 
a^{b \pmod{\phi(m)}} & \gcd(a, m)=1 \\
a^{b \pmod{\phi(m)} + \phi(m)} & \gcd(a, m) \ne 1, b > \phi(m)
\end{cases} \pmod m
$$