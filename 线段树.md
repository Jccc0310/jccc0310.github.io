# 线段树

## 0x00 线段树基础

### 1. 线段树的基本结构

线段树将每个长度不为 $1$ 的区间划分成左右两个区间递归求解，把整个线段划分为一个树形结构，通过合并左右两区间信息来求得该区间的信息。

这种数据结构可以方便的进行大部分的**区间操作**。

线段树的形态，如图所示：
![线段树的基本结构](https://oi-wiki.org/ds/images/segt1.svg)


### 2. 维护信息的特性

#### Ⅰ. 区间结合律 —— **最核心特性**

假设一个区间被拆分为左子区间 **$L$** 和右子区间 **$R$**，那么整个区间的属性值 **$v$** 必须满足：

$$
v(L \cup R) = v(L) \text{ op } v(R)
$$

其中 **$\text{op}$** 是某种二元运算必须满足：**$(A \text{ op } B) \text{ op } C = A \text{ op } (B \text{ op } C)$**。

#### Ⅱ. 区间可合并性

父节点的信息必须能由子节点**直接**或**间接**推导出来。

#### Ⅲ. 对于区间修改：懒标记的“分配律”与“叠加性”

在线段树中，我们维护区间信息 $V$（如区间和、向量）和修改操作 $F$（懒标记）。要让区间修改高效工作，必须满足两个核心性质：

- **对信息的分配律**：
把标记打在父节点上，等价于分别打在两个子节点上再合并，即：

$$
F(V_L \text{ op } V_R) = F(V_L) \text{ op } F(V_R)
$$

- **标记的可叠加性**：对于两次修改 $F_1$ 和 $F_2$，必须存在一种运算 $\otimes$ 使得：

$$
F_2(F_1(V)) = (F_2 \otimes F_1)(V)
$$

这里的 $F_{new} = F_2 \otimes F_1$ 就是我们要维护的合并标记。

#### Ⅳ. 常见维护信息的总结

- **可维护信息表**（满足结合律）

| 信息类型 | 结合律 (PushUp) 逻辑 | 向量/矩阵维度 | 提炼公式 |
| :--- | :--- | :--- | :--- |
| **区间和 (Sum)** | `u.s = l.s + r.s` | $[S, 1]^T$ | $S' = aS + bL$ |
| **最值 (Max/Min)** | `u.v = max(l.v, r.v)` | $(\max, +)$ 算子 | $V' = V + b$ |
| **区间 GCD** | `u.g = gcd(l.g, r.g)` | 不建议矩阵 | 区间修改通常需结合**差分**转化为单点修改 |
| **异或和 (XOR)** | `u.x = l.x ^ r.x` | $[X]$ | $X' = (len \% 2) ? (X \oplus v) : X$ |
| **最大子段和** | 需维护 `lmax, rmax, sum, res` | $[f, g, 0]^T$ | `u.res = max({l.res, r.res, l.rmax + r.lmax})` |
| **区间平方和** | `u.s2 = l.s2 + r.s2` | $[S^2, S, 1]^T$ | $S_2' = S_2 + 2vS_1 + v^2L$ |
| **括号匹配** | 需维护 `l` (未), `r` (未), `cnt` (已) | 逻辑判定 | `u.cnt = l.cnt + r.cnt + min(l.l, r.r)` |
| **区间哈希** | `u.h = (l.h * base^r.len + r.h) % MOD` | $[H, 1]^T$ | 常用于动态维护字符串/回文判断 |

- **不可维护信息表**

| 信息类型 | 为什么线段树不行 | 常见替代方案 |
| :--- | :--- | :--- |
| **区间众数 (Mode)** | 不满足结合律。左右区间的众数无法决定大区间众数。 | **莫队算法**、**分块** |
| **区间中位数** | 无法通过简单的 $O(1)$ 合并推导。 | **主席树**（可持久化线段树） |
| **区间不同数个数** | 无法处理重复项对合并的贡献（去重逻辑不可合并）。 | **主席树**、**树状数组+离线**、**莫队** |
| **最长上升子序列** | 合并两个区间的 LIS 复杂度极高，需维护过多状态。 | **分块**、**平衡树维护 DP** |

---

## 0x01 矩阵乘法推导懒标记转移

### 1. 定义“向量”和“矩阵”

在线段树的一个节点里，我们要维护区间的和 $S$。因为修改操作里有常数项 $b$，所以我们需要维护区间长度 $L$ 作为齐次坐标。

- **状态向量** $V = \begin{bmatrix} S \\ L \end{bmatrix}$

- **转移矩阵** $M = \begin{bmatrix} a & b \\ 0 & 1 \end{bmatrix}$ （代表操作 $x \to ax + b$）

### 2. 矩阵乘法

$$
V_{new} = M \times V_{old} = \begin{bmatrix} a & b \\ 0 & 1 \end{bmatrix} \times \begin{bmatrix} S \\ L \end{bmatrix} = \begin{bmatrix} a \cdot S + b \cdot L \\ L \end{bmatrix}
$$

### 3. 提炼“标记合并”逻辑

如果你已经在节点上有一个标记 $M_{old}$（代表先做的操作），现在又从父节点传下来一个标记 $M_{new}$（代表后做的操作），合并后的总标记 $M_{total}$ 是多少？

**注意矩阵乘法顺序**：$M_{total} = M_{new} \times M_{old}$。

$$
M_{total} = \begin{bmatrix} a_{new} & b_{new} \\ 0 & 1 \end{bmatrix} \times \begin{bmatrix} a_{old} & b_{old} \\ 0 & 1 \end{bmatrix} = \begin{bmatrix} a_{new} \cdot a_{old} & a_{new} \cdot b_{old} + b_{new} \\ 0 & 1 \end{bmatrix}
$$

提炼出的有用转移（看结果矩阵的第一行）：
1. 新的乘法标记 = $a_{new} \cdot a_{old}$
2. 新的加法标记 = $a_{new} \cdot b_{old} + b_{new}$

### 4. 矩阵推导的优势

便于快速推导出复杂区间修改的懒标记合并逻辑，避免了复杂的逻辑推理。

---

## 0x02 普通线段树

### 1. 定义

没啥可说的。

### 2. 模板

``` cpp
#include <bits/stdc++.h>
using namespace std;

struct SegTree {
    struct Node {
        int sum, tag;
    };
    int n;
    vector<Node> t;

    SegTree(int _n) : n(_n) {
        t.resize(n * 4 + 1, {0, 0});
    }

    void push_up(int u) {
        t[u].sum = t[u << 1].sum + t[u << 1 | 1].sum;
    }

    void push_down(int u, int l, int r) {
        if (t[u].tag) {
            int mid = (l + r) >> 1;
            t[u << 1].sum += t[u].tag * (mid - l + 1);
            t[u << 1].tag += t[u].tag;
            t[u << 1 | 1].sum += t[u].tag * (r - mid);
            t[u << 1 | 1].tag += t[u].tag;
            t[u].tag = 0;
        }
    }

    void build(int u, int l, int r, const vector<int>& a) {
        if (l == r) {
            t[u].sum = a[l];
            return;
        }
        int mid = (l + r) >> 1;
        build(u << 1, l, mid, a);
        build(u << 1 | 1, mid + 1, r, a);
        push_up(u);
    }

    void change(int u, int l, int r, int ql, int qr, int v) {
        if (ql <= l && r <= qr) {
            t[u].sum += v * (r - l + 1);
            t[u].tag += v;
            return;
        }
        push_down(u, l, r);
        int mid = (l + r) >> 1;
        if (ql <= mid) change(u << 1, l, mid, ql, qr, v);
        if (qr > mid) change(u << 1 | 1, mid + 1, r, ql, qr, v);
        push_up(u);
    }

    int query(int u, int l, int r, int u_l, int u_r) {
        if (u_l <= l && r <= u_r) return t[u].sum;
        push_down(u, l, r);
        int mid = (l + r) >> 1, res = 0;
        if (u_l <= mid) res += query(u << 1, l, mid, u_l, u_r);
        if (u_r > mid) res += query(u << 1 | 1, mid + 1, r, u_l, u_r);
        return res;
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin >> n >> m;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];

    // 实例化对象
    SegTree st(n);
    st.build(1, 1, n, a);

    while (m--) {
        int op, l, r, k;
        cin >> op >> l >> r;
        if (op == 1) {
            cin >> k;
            st.change(1, 1, n, l, r, k);
        } else {
            cout << st.query(1, 1, n, l, r) << "\n";
        }
    }

    return 0;
}
```

---

## 0x03 动态开点线段树

### 1. 定义

等待补充ing...

### 2. 模板

``` cpp
#include <bits/stdc++.h>
using namespace std;

#define ll long long

const ll L = 1, R = 1e9; // 默认值域

struct SegTree {
    struct Node {
        ll sum, tag;
        int ls, rs;
    };
    vector<Node> t;
    int root, cnt;

    SegTree() {
        t.push_back({0, 0, 0, 0}); // 0号节点充当空节点
        root = 0;
        cnt = 0;
    }

    int newNode() {
        t.push_back({0, 0, 0, 0});
        return ++cnt;
    }

    void push_up(int u) {
        t[u].sum = t[t[u].ls].sum + t[t[u].rs].sum;
    }

    void push_down(int u, ll l, ll r) {
        if (!t[u].ls) t[u].ls = newNode();
        if (!t[u].rs) t[u].rs = newNode();
        if (t[u].tag) {
            ll mid = (l + r) >> 1;
            t[t[u].ls].sum += t[u].tag * (mid - l + 1);
            t[t[u].ls].tag += t[u].tag;
            t[t[u].rs].sum += t[u].tag * (r - mid);
            t[t[u].rs].tag += t[u].tag;
            t[u].tag = 0;
        }
    }

    void update(int &u, ll l, ll r, ll ul, ll ur, int v) {
        if (!u) u = newNode();
        if (ul <= l && r <= ur) {
            t[u].sum += (ll)v * (r - l + 1);
            t[u].tag += v;
            return;
        }
        push_down(u, l, r);
        ll mid = (l + r) >> 1;
        if (ul <= mid) update(t[u].ls, l, mid, ul, ur, v);
        if (ur > mid) update(t[u].rs, mid + 1, r, ul, ur, v);
        push_up(u);
    }

    ll query(int u, ll l, ll r, ll ul, ll ur) {
        if (!u) return 0;
        if (ul <= l && r <= ur) return t[u].sum;
        push_down(u, l, r);
        ll mid = (l + r) >> 1, res = 0;
        if (ul <= mid) res += query(t[u].ls, l, mid, ul, ur);
        if (ur > mid) res += query(t[u].rs, mid + 1, r, ul, ur);
        return res;
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int m;
    cin >> m;
    SegTree st;

    while (m--) {
        int op;
        ll l, r;
        cin >> op >> l >> r;
        if (op == 1) {
            int k;
            cin >> k;
            st.update(st.root, L, R, l, r, k);
        } else {
            cout << st.query(st.root, L, R, l, r) << "\n";
        }
    }

    return 0;
}
```

---

## 0x04 可持久化线段树

### 1. 定义

等待补充ing...

### 2. 模板

``` cpp
#include <bits/stdc++.h>
using namespace std;

#define ll long long

struct SegTree {
    struct Node {
        int sum, ls, rs; // 左右儿子不再是 2u, 2u+1，而是存编号
    };
    vector<Node> t;
    vector<int> roots;
    int n, cnt;

    SegTree(int _n) : n(_n), cnt(0) {
        // 预估空间：n(初始) + m * log(n)
        t.push_back({0, 0, 0}); // 0号节点
    }

    int newNode(int old = 0) {
        t.push_back(t[old]); // 复制旧节点信息
        return ++cnt;
    }

    void push_up(int u) {
        t[u].sum = t[t[u].ls].sum + t[t[u].rs].sum;
    }

    // 构建初始版本（版本 0）
    int build(int l, int r, const vector<int>& a) {
        int u = newNode();
        if (l == r) {
            t[u].sum = a[l];
            return u;
        }
        int mid = (l + r) >> 1;
        t[u].ls = build(l, mid, a);
        t[u].rs = build(mid + 1, r, a);
        push_up(u);
        return u;
    }

    // 单点修改：在 old 版本基础上将 p 位置加 v，返回新版本的根
    int update(int old, int l, int r, int p, int v) {
        int u = newNode(old); // 关键：复制路径上的节点
        if (l == r) {
            t[u].sum += v;
            return u;
        }
        int mid = (l + r) >> 1;
        if (p <= mid) t[u].ls = update(t[old].ls, l, mid, p, v);
        else t[u].rs = update(t[old].rs, mid + 1, r, p, v);
        push_up(u);
        return u;
    }

    int query(int u, int l, int r, int ql, int qr) {
        if (!u || (ql <= l && r <= qr)) return t[u].sum;
        int mid = (l + r) >> 1, res = 0;
        if (ql <= mid) res += query(t[u].ls, l, mid, ql, qr);
        if (qr > mid) res += query(t[u].rs, mid + 1, r, ql, qr);
        return res;
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin >> n >> m;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];

    SegTree st(n);
    st.roots.push_back(st.build(1, n, a)); // 初始版本存入 roots[0]

    while (m--) {
        int op, v, l, r, p, k;
        cin >> op;
        if (op == 1) { // 在版本 v 的基础上，修改位置 p 的值为 k，生成新版本
            cin >> v >> p >> k;
            st.roots.push_back(st.update(st.roots[v], 1, n, p, k));
        } else { // 查询版本 v 中区间 [l, r] 的和
            cin >> v >> l >> r;
            cout << st.query(st.roots[v], 1, n, l, r) << "\n";
        }
    }

    return 0;
}
```
